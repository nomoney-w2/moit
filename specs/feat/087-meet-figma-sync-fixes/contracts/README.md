# API Contracts

**해당 없음.**

본 명세 `087-meet-figma-sync-fixes` 는 클라이언트 UI/검증 폴리시 한정이며, 신규 API 엔드포인트 추가나 기존 엔드포인트 시그니처 변경이 없다. 따라서 별도 contract 파일이 생성되지 않는다.

- `POST /v1/meeting` 의 `meetingName` 필드에 공백 문자가 포함될 수 있다는 점만 의미상 변경됨. 백엔드 측 스키마는 이미 임의 문자열을 수용하므로 별도 협업 불필요.
- 기타 모든 엔드포인트 동작 유지.
